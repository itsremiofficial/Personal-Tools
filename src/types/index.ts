export interface IconProps {
  className?: string;
  fill?: boolean;
  duotone?: boolean;
  width?: string | number;
}

export interface SmoothProps {
  className?: string; 
  children?: React.ReactNode;
}
